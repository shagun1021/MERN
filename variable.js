// variable in js ->
// 1- let
// 2- var3- const

{
      var x=10;
      let y=20;
}

console.log(x);
// console.log(y); // can not use outside the block because let is a block scope variable

const z=30;
// z=23;  // Error: const value can not be changed
console.log(z);